# 🔒 安全策略

## 1. 安全承诺

本项目以**私有化部署、数据不出内网**为核心安全目标：

- 密钥（`LLM_API_KEY`）只存于 `backend/.env`（已 gitignore），**绝不入库、不写日志**；
- 上传文件路径清洗 + 防路径穿越；文档解析做编码容错；
- 前端 Markdown 渲染**关闭 HTML**，防存储型 XSS；
- 支持 Docker 内网部署，数据与索引全量留在本地。

## 2. 支持的版本

| 版本 | 支持状态 |
|------|----------|
| 2.x（当前主线，FastAPI 重构版） | ✅ 受支持，建议保持最新 |
| 1.x 及更早（初版） | ❌ 不再维护 |

## 3. 报告漏洞

如发现安全问题，**请不要公开发布 Issue**，请私下联系维护者：

- 邮箱：*（维护者在此填写联系方式）*

报告中请说明：问题描述与影响面、复现步骤、建议修复方案（可选）。
我们会在 3 个工作日内响应，并在修复后统一致谢。

## 4. 部署前必读

- **不要**把 `backend/.env` 或其中密钥提交到公开仓库；
- 生产环境建议收紧 `CORS`（默认 `*`，见 `app/core/config.py`），并在 Nginx 层加 HTTPS 与访问控制（见 [DEPLOYMENT.md](./docs/DEPLOYMENT.md)）；
- 企业文档可能含敏感信息，请自行确认上传内容的使用边界。
